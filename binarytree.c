#include <stdio.h>

typedef struct {
    char * word;
    struct node * left;
    struct node * right;
} node;

void add(node ** root) {
    if (root == NULL) {
        
    }
}


int main() {
    node * root = NULL;


    return 0;
}